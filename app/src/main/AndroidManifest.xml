<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:usesCleartextTraffic="false"
        android:allowBackup="true"
        android:label="FairSplit"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FairSplit">

        <activity
            android:name="com.example.fairsplit.view.SettingsActivity"
            android:exported="false" />

        <activity
            android:name="com.example.fairsplit.view.ExpensesActivity"
            android:exported="false" />

        <activity
            android:name="com.example.fairsplit.view.GroupsActivity"
            android:exported="false" />

        <activity
            android:name="com.example.fairsplit.view.RegisterActivity"
            android:exported="false" />

        <activity
            android:name="com.example.fairsplit.view.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
